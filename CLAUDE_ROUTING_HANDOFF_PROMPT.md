Claude Code에서 사용할 작업별 모델·노력 수준 자동 분담을 조사하고, 실제로 동작하는 최소 훅/하위 에이전트 구성을 구현해줘.

이 요청의 대상은 Claude Code다. Codex와는 별도로 같은 목표의 모델 라우팅을 만들려는 것이다. Codex의 설정·훅·작업을 동시에 수정하지 마라. 현재 프로젝트 앱의 기사 생성 API 모델을 교체하는 작업도 아니다.

내 목표와 지금까지의 논의:
- 모든 작업을 최고 모델·최고 effort로 처리하는 대신, 작업에 필요한 수준을 자동으로 골라 높은 품질을 유지하면서 완료까지의 토큰·구독 사용량·시간을 줄이고 싶다.
- 예전에 “계획·설계는 Opus, 구현은 Sonnet”으로 나누던 opusplan을 떠올려 Codex와 조사했다. 이제는 작업을 더 구체적으로 분류하고, 하위 작업 생성 시 적절한 모델·effort를 배정하는 방식을 원한다.
- 최저가 모델 사용 자체가 목적이 아니다. 재작업·오류·검토 부담까지 포함한 완료당 소모가 중요하다. 품질을 낮추거나 필요한 검증을 빼서 절약하면 안 된다.
- 매번 /model이나 /effort를 직접 바꾸고 질문을 재전송하는 방식은 원하지 않는다. 같은 대화에서 자연스럽게 이어져야 한다.

현재까지 관찰한 환경(현재 상태를 다시 확인할 것):
- Windows, PowerShell. 2026-09-10 관찰 당시 Claude Code CLI2.1.114.
- C:\Users\dsaka\.claude\settings.json에 model=claude-opus-4-8, effortLevel=xhigh가 저장돼 있었다. 저장값이지 실제 모든 실행의 모델을 검증한 결과는 아니다.
- 기존 UserPromptSubmit/PostToolUse 훅과 Ponytail 플러그인이 있다. 기존 훅·권한·MCP·사용자 설정을 보존하고 필요한 항목만 병합해라.
- 작업 폴더는 C:\Users\dsaka\Desktop\Segye Casefile. 이곳의 AGENTS.md, HANDOFF.md, CONTINUE_PROMPT.md를 먼저 읽어라. 모델 라우팅 조사는 MODEL_ROUTING_RESEARCH.md, 최신 Codex 배정안은 SUBAGENT_ROUTING_POLICY.md에 있다. 모두 조사/제안이며 훅은 아직 구현·설치하지 않았다.
- 위 프로젝트에 다른 개발 작업의 미커밋 변경이 많다. 원고/앱 코드/서비스/평가 데이터를 건드리지 마라. 전역 Claude 도구 설정 작업과 프로젝트 제품 작업을 분리해라.

기존 조사에서 확인한 기능과 주의점:
1. Claude 공식 opusplan은 Plan Mode에서 Opus, 실행에서는 Sonnet을 사용한다. 질문 난도를 매번 분류하는 기능과는 다르다.
2. 공식 advisor는 Sonnet 등이 일하다 필요할 때 Opus에 자문하는 실험 기능이다. 최신 문서는 구독 계정도 지원하지만 로컬 설치 버전·계정에서 되는지는 확인하지 않았다. 최신 desktop/headless /advisor 명령은 v2.1.260 이상을 요구한다. --advisor가 help에 안 나오는 것만으로 미지원이라고 단정하면 안 된다.
3. tzachbon/claude-model-router-hook 최신 소스는 변경이 필요하면 입력을 중단하고 /model·/effort 후 재전송을 요구하는 경로가 있어서 그대로 설치할 후보에서 제외했다.
4. Codex에서는 PreToolUse가 spawn_agent/Agent 인수를 바꾸는 공식 경로를 확인했다. 이 사실을 Claude에 그대로 적용하지 말고 Claude의 현재 훅 스키마·Agent 도구·subagent frontmatter·설정 우선순위를 직접 확인해라. 주 모델 변경, 하위 모델 선택, effort 변경은 각각 지원 여부를 구분해라.
5. UserPromptSubmit의 additionalContext에 “모델을 바꿔라”는 문구를 넣었다고 실제 모델이 바뀐 것으로 보고하면 안 된다. 설정 파일에 쓴 값과 실행 시 실제 모델·effort가 일치하는지 검증해야 한다.

네가 할 일:
1. 최신 Claude 공식 문서와 실제 사용자 운영 사례·원저자의 비교 실험을 읽어 Haiku/Sonnet/Opus 및 현재 접근 가능한 모델의 작업별 effort 배정안을 정해라. 후기, 작은 실험, 공식 지원 사실을 구분하고 출처/날짜/한계를 기록해라. 이미 조사한 내용을 불필요하게 처음부터 전부 반복하지 말되 Codex 모델의 결과를 Claude에 전용하지 마라.
2. 기계적인 명령 실행, 정확한 위치/필드 찾기, 문서 정리, 호출 흐름 분석, 한정 구현, 다중 파일 수정, 테스트 작성, UI 구현, 깊은 디버깅, 보안/데이터 무결성 검토, 설계로 작업을 나눠라. 읽기 전용이라고 모두 쉬운 작업으로 분류하지 마라.
3. 기존 주 모델이 맥락을 보고 역할을 판단하고, 훅/역할 설정이 정해진 모델·effort를 적용하는 단순 구조를 우선 검토해라. 별도 분류 모델 API나 프록시 없이 되는 방식을 우선한다. opusplan·advisor·공식 subagent 설정이 필요한 부분을 이미 해결하면 재사용해라.
4. 실제 지원되는 부분을 구현해라. 훅의 실제 생성 이벤트·필드와 effort 설정 위치를 확인한 뒤, Python 표준 라이브러리나 이미 설치된 런타임으로 최소한의 코드와 설정을 작성해라. 막연한 설계 보고서에서 멈추지 마라. 지원되지 않는 주 모델 실시간 전환을 작동하는 것처럼 꾸미지 말고, 가능한 하위 작업 분담을 완성해라.
5. 훅을 먼저 합성 입력으로 검증하고, 기존 Claude 설정을 백업한 뒤 필요한 항목만 병합해 설치해라. 스크립트 직접 실행 성공과 실제 Claude 이벤트 실행 성공을 구분해라. 작은 비민감 샘플로 실제 선택된 모델/effort와 기존 훅의 공존을 확인해라. 별도 유료 API·외부 게이트웨이·계정 구매는 임의로 도입하지 마라. 일반 세션 사용량과 추가 과금을 구분해라.
6. 원복 방법, 변경 파일, 모델 배정표, 실제 검증 결과와 미검증 범위를 간결하게 보고해라. 도구 권한이나 필수 재시작으로 막히면 확인한 사실을 설명하고 가능한 나머지 작업은 마무리해라.

정책에 반드시 반영할 것:
- 사용자 명시 모델·effort·범위·검증 요구가 우선한다. 짧은 “응 진행해” 같은 후속 입력은 직전 작업 맥락을 이어받는다. 키워드 하나로 임의 하향하지 마라.
- 별도 모델을 만들 필요 없는 짧은 명령·검사 실행은 현재 에이전트의 도구로 처리한다. 독립적으로 완료할 수 있는 의미 있는 작업만 위임하고, 감독이 같은 조사를 중복하지 않게 한다.
- 필요한 요구·금지사항·파일·근거·완료검사를 자식에게 전달한다. 문맥 복사를 줄이더라도 근거를 누락하거나 원문을 임의 절삭하지 마라.
- 작은 모델을 항상 max로 설정하거나 모든 모델을 순서대로 재시도하지 마라. 검증 실패가 실제 추론 한계인지, 자료/환경/테스트 결함인지 구분해 상향해라. 중요한 설계·검토는 처음부터 충분한 수준을 배정한다.
- 불필요한 상태 폴링·재귀 위임·과도한 병렬 작업을 피하고 완료 알림/적절한 도구 대기를 활용해라. 필요한 진행 설명은 유지해라.
- 요청 모델, 실제 모델, effort, 분류 이유의 짧은 요약, 시간, 입력/캐시/출력/추론 사용량과 검증 결과를 기록하되 키·토큰·쿠키·기사 전문·민감한 대화 본문·비공개 사고 과정을 로그에 남기지 마라. 원시 토큰, 과금/credits, 구독 한도는 구분하고 추론/출력 및 누적값을 중복 집계하지 마라.
- 역할에 낮은 모델을 지정한 것만으로 절감·품질을 주장하지 마라. 감독·작업자·검토·실패/재시도까지 합친 수락 결과당 전체 소모로 평가해라. 모델에 따라 지원하지 않는 effort나 도구를 강제로 설정하지 마라.
- 토큰 절약을 이유로 권한을 넓히거나 필수 테스트·접근성·출처검증·전문보존을 줄이지 마라. 오래된 커뮤니티 설정의 임의 context_window/카탈로그 패치를 무검증 복사하지 마라.
- 현재 세션을 유지하는 것이 기본이다. 단계 종료나 대화 길이만으로 새 세션을 권하지 마라. 사용자 지시 없는 새 작업 생성·커밋·서비스 종료·발행은 하지 마라. 중요한 작업이 끝나면 기존 규약에 따라 핸드오프를 갱신하되 다른 작업 기록을 덮어쓰지 마라.

출발점 공식 문서:
https://code.claude.com/docs/en/hooks
https://code.claude.com/docs/en/sub-agents
https://code.claude.com/docs/en/model-config
https://code.claude.com/docs/en/advisor

이 요청은 위 범위의 Claude Code용 구현·백업·설치·검증을 진행하라는 지시다. 통상적인 되돌릴 수 있는 작업은 매 단계 재확인하지 말고 완료해라. 실제 권한 경계나 추가 외부 과금처럼 별도 확인이 필요한 경우에만 그 이유와 필요한 최소 사항을 설명해라.
